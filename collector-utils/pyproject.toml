[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "openhound-collector-utils"
version = "0.0.1"
description = "Reusable target queue for OpenHound collectors"
requires-python = ">=3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/openhound_collector_utils"]
